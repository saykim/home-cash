# 디자인 시스템 가이드

<!--
  ╔═══════════════════════════════════════════════════════════════════════════╗
  ║  🎨 CUSTOMIZATION GUIDE (커스터마이징 가이드)                              ║
  ╠═══════════════════════════════════════════════════════════════════════════╣
  ║  이 문서는 범용 디자인 시스템 템플릿입니다.                                ║
  ║  새 프로젝트에서 사용할 때 아래 표시된 영역을 수정하세요:                   ║
  ║                                                                           ║
  ║  [CUSTOMIZE: ...] - 프로젝트에 맞게 수정 필요                             ║
  ║  [OPTIONAL: ...]  - 도메인에 따라 추가/삭제 가능                          ║
  ║  [THEME: ...]     - 테마/브랜드 컬러 커스터마이징                         ║
  ╚═══════════════════════════════════════════════════════════════════════════╝
-->

<!-- [CUSTOMIZE: 프로젝트 이름으로 변경] -->
> **Home-Cash** - 일관성 있는 UI/UX를 위한 디자인 시스템 가이드

## 📋 목차

- [구현 상태 검토](#구현-상태-검토)
- [컬러 시스템](#컬러-시스템)
- [타이포그래피](#타이포그래피)
- [레이아웃](#레이아웃)
- [컴포넌트](#컴포넌트)
- [다크모드](#다크모드)
- [접근성](#접근성)

---

## 📊 구현 상태 검토

> 마지막 검토: 2025-12-29

### 전체 요약

| 항목 | 상태 | 비고 |
|------|------|------|
| 디자인 시스템 정의 | 🟢 우수 | index.css 및 상수 잘 정돈됨 |
| 공통 컴포넌트 | 🟢 우수 | PageHeader, EmptyState, KpiCard 구현 완료 |
| Tailwind Config | 🟢 완료 | 도메인/상태 색상 연결 완료 |
| HomePage 구현 | 🟢 완료 | PageHeader, DashboardKpiCard 적용 |
| TransactionsPage | 🟢 완료 | PageHeader 적용 |

### 1. 구성 요소 (Configuration)

#### ✅ CSS Variables (`/src/index.css`)

```css
/* 정의 완료된 변수들 */
:root {
  /* 브랜드 컬러 */
  --primary, --secondary

  /* 시맨틱 컬러 */
  --status-success, --status-warning, --status-danger, --status-info

  /* 도메인 컬러 (재무) */
  --income, --expense, --transfer
}
```

#### ✅ Tailwind Config (`/tailwind.config.ts`)

도메인/상태 색상이 extend.colors에 연결되어 `bg-income`, `text-expense`, `border-status-success` 같은 Tailwind 클래스로 사용 가능합니다.

```ts
// tailwind.config.ts
colors: {
  income: 'hsl(var(--income))',
  expense: 'hsl(var(--expense))',
  transfer: 'hsl(var(--transfer))',
  status: {
    success: 'hsl(var(--status-success))',
    warning: 'hsl(var(--status-warning))',
    danger: 'hsl(var(--status-danger))',
    info: 'hsl(var(--status-info))',
  }
}
```

### 2. 공통 컴포넌트

| 컴포넌트 | 위치 | 상태 |
|----------|------|------|
| PageHeader | `/src/components/common/PageHeader.tsx` | ✅ 구현 완료 |
| EmptyState | `/src/components/common/EmptyState.tsx` | ✅ 구현 완료 |
| KpiCard | `/src/components/common/KpiCard.tsx` | ✅ 구현 완료 |
| DashboardKpiCard | `/src/components/home/DashboardKpiCard.tsx` | ✅ 구현 완료 |

### 3. 페이지별 구현 현황

#### HomePage.tsx (대시보드)

| 항목 | 상태 | 구현 내용 |
|------|------|----------|
| PageHeader 사용 | ✅ | 제목 + 날짜 description |
| KPI 카드 컴포넌트화 | ✅ | DashboardKpiCard 컴포넌트 사용 |
| EmptyState 사용 | ✅ | 자산, 거래, 결제 예정 빈 상태 |
| 하드코딩 스타일 제거 | ✅ | 시맨틱 컬러 사용 |

#### TransactionsPage.tsx

| 항목 | 상태 | 구현 내용 |
|------|------|----------|
| PageHeader 사용 | ✅ | action에 PeriodNavigator 전달 |
| TransactionSummary 사용 | ✅ | KpiCard 재사용 |

#### 기타 페이지

- **BudgetPage**: ✅ PageHeader 사용
- **StatisticsPage**: ✅ PageHeader 사용
- **CardsPage**: ✅ EmptyState 사용
- **CherryPickerPage**: ✅ EmptyState 사용

---

## 🎨 컬러 시스템

### CSS 변수 사용 원칙

**✅ DO: CSS 변수 사용**
```tsx
// Good: CSS 변수 사용
<div className="bg-primary text-primary-foreground" />
<circle stroke="hsl(var(--status-success))" />
```

**❌ DON'T: 하드코딩된 컬러 값**
```tsx
// Bad: 하드코딩 금지
<div style={{ color: '#ef4444' }} />
<circle stroke="#22c55e" />
```

### 브랜드 컬러

<!--
  ╔═══════════════════════════════════════════════════════════════════════════╗
  ║  [THEME: 브랜드 컬러]                                                      ║
  ║  프로젝트의 브랜드 아이덴티티에 맞게 primary, secondary 컬러를 설정하세요.  ║
  ║  HSL 형식: H(색상) S(채도)% L(밝기)%                                       ║
  ║                                                                           ║
  ║  예시:                                                                    ║
  ║  - 블루 테마: 221 83% 53%                                                 ║
  ║  - 그린 테마: 142 76% 36%                                                 ║
  ║  - 퍼플 테마: 262 83% 58%                                                 ║
  ║  - 오렌지 테마: 24 95% 53%                                                ║
  ╚═══════════════════════════════════════════════════════════════════════════╝
-->

```css
/* /src/index.css */
:root {
  /* [THEME: Primary 브랜드 컬러 - 메인 액센트 색상] */
  --primary: 221 83% 53%;           /* 현재: 블루 */
  --primary-foreground: 210 40% 98%;

  /* [THEME: Secondary 브랜드 컬러 - 보조 색상] */
  --secondary: 210 40% 96.1%;
  --secondary-foreground: 222.2 47.4% 11.2%;
}

.dark {
  /* [THEME: 다크모드 Primary - 밝기 조정 필요] */
  --primary: 330 81% 70%;           /* 다크모드에서 밝게 */
  --primary-foreground: 222.2 47.4% 11.2%;
}
```

### 시맨틱 컬러 (Status)

<!-- 이 섹션은 범용적으로 사용 가능합니다 -->

```css
:root {
  /* Status Colors - 대부분의 프로젝트에서 공통 사용 */
  --status-success: 142 76% 36%;  /* 성공: 초록 */
  --status-warning: 38 92% 50%;   /* 경고: 주황 */
  --status-danger: 0 84% 60%;     /* 위험: 빨강 */
  --status-info: 217 91% 60%;     /* 정보: 파랑 */
}

.dark {
  /* Dark Mode - 밝기 증가 */
  --status-success: 142 70% 45%;
  --status-warning: 38 90% 60%;
  --status-danger: 0 84% 70%;
  --status-info: 217 91% 70%;
}
```

### 도메인 특화 컬러

<!--
  ╔═══════════════════════════════════════════════════════════════════════════╗
  ║  [OPTIONAL: 도메인 특화 컬러]                                              ║
  ║  프로젝트 도메인에 맞는 시맨틱 컬러를 정의하세요.                           ║
  ║                                                                           ║
  ║  예시:                                                                    ║
  ║  - 재무 앱: income, expense, transfer                                     ║
  ║  - 건강 앱: healthy, warning, critical                                    ║
  ║  - 교육 앱: correct, incorrect, partial                                   ║
  ║  - 이커머스: sale, soldout, new                                           ║
  ║                                                                           ║
  ║  도메인 특화 컬러가 필요 없으면 이 섹션을 삭제하세요.                       ║
  ╚═══════════════════════════════════════════════════════════════════════════╝
-->

```css
/* 현재 프로젝트: 재무 앱 (Financial) */
:root {
  /* [OPTIONAL: 재무 도메인 컬러] */
  --income: 142 76% 36%;      /* 수입: 초록 */
  --expense: 0 84% 60%;        /* 지출: 빨강 */
  --transfer: 217 91% 60%;     /* 이체: 파랑 */
}
```

### Tailwind 유틸리티 클래스

<!--
  ╔═══════════════════════════════════════════════════════════════════════════╗
  ║  [OPTIONAL: 도메인 특화 유틸리티 클래스]                                    ║
  ║  도메인 컬러에 맞는 유틸리티 클래스를 정의하세요.                           ║
  ╚═══════════════════════════════════════════════════════════════════════════╝
-->

```css
/* /src/index.css - @layer components */
@layer components {
  /* [OPTIONAL: 재무 앱 전용 클래스] */
  .amount-income {
    @apply text-green-600 dark:text-green-400;
  }
  .amount-expense {
    @apply text-red-600 dark:text-red-400;
  }
  .amount-transfer {
    @apply text-blue-600 dark:text-blue-400;
  }
}
```

---

## 📝 타이포그래피

### 폰트 패밀리

<!--
  ╔═══════════════════════════════════════════════════════════════════════════╗
  ║  [THEME: 폰트 패밀리]                                                      ║
  ║  브랜드에 맞는 폰트를 선택하세요.                                          ║
  ║                                                                           ║
  ║  한글 추천 폰트:                                                          ║
  ║  - Pretendard (무료, 가변폰트)                                            ║
  ║  - Noto Sans KR (무료, Google Fonts)                                      ║
  ║  - Spoqa Han Sans (무료)                                                  ║
  ║  - IBM Plex Sans KR (무료)                                                ║
  ║                                                                           ║
  ║  영문 추천 폰트:                                                          ║
  ║  - Inter (무료, 가변폰트)                                                 ║
  ║  - Outfit (제목용, 무료)                                                  ║
  ║  - IBM Plex Sans (무료)                                                   ║
  ╚═══════════════════════════════════════════════════════════════════════════╝
-->

```css
/* [THEME: 본문 폰트] */
body {
  font-family: 'IBM Plex Sans', -apple-system, BlinkMacSystemFont, sans-serif;
}

/* [THEME: 제목 폰트 - 선택적으로 다른 폰트 사용] */
h1, h2, h3, h4, h5, h6 {
  font-family: 'Outfit', 'IBM Plex Sans', sans-serif;
  font-weight: 700;
  letter-spacing: -0.02em;
}
```

### 타이포그래피 스케일

<!-- 이 섹션은 범용적으로 사용 가능합니다 -->

| 용도 | 클래스 | 예시 |
|------|--------|------|
| 페이지 제목 | `text-2xl font-bold` | 메인 페이지 타이틀 |
| 카드 제목 (대) | `text-lg font-semibold` | 섹션 타이틀 |
| 카드 제목 (소) | `text-sm font-semibold` | 작은 카드 타이틀 |
| 본문 | `text-base` | 일반 텍스트 |
| 보조 텍스트 | `text-sm text-muted-foreground` | 설명, 도움말 |
| 캡션 | `text-xs text-muted-foreground` | 메타 정보 |

### 숫자 표시

<!--
  ╔═══════════════════════════════════════════════════════════════════════════╗
  ║  [OPTIONAL: 숫자/금액 표시]                                                ║
  ║  금융, 통계, 대시보드 앱에서 숫자 정렬이 중요한 경우 사용하세요.            ║
  ╚═══════════════════════════════════════════════════════════════════════════╝
-->

```tsx
// 숫자/금액은 tabular-nums로 정렬 일관성 유지
<div className="text-2xl font-bold tabular-nums">
  {formatCurrency(amount)}
</div>
```

---

## 📐 레이아웃

### 표준 그리드 패턴

<!-- 이 섹션은 범용적으로 사용 가능합니다 -->

```tsx
import { LAYOUTS } from '@/lib/constants/layouts';

// 2열 그리드 (lg:1024px+에서 2열)
<div className={LAYOUTS.twoColumn}>
  {/* content */}
</div>

// 3열 그리드 (md:768px+에서 2열, lg:1024px+에서 3열)
<div className={LAYOUTS.threeColumn}>
  {/* content */}
</div>

// 대시보드 그리드 (12열 기반 유연한 레이아웃)
<div className={LAYOUTS.dashboard}>
  {/* content */}
</div>
```

### 간격 (Spacing)

<!--
  ╔═══════════════════════════════════════════════════════════════════════════╗
  ║  [THEME: 간격 값]                                                          ║
  ║  프로젝트의 밀도(density)에 따라 조정하세요.                               ║
  ║                                                                           ║
  ║  - 컴팩트 UI: gap-4, space-y-4                                            ║
  ║  - 기본 UI: gap-6, space-y-6                                              ║
  ║  - 여유로운 UI: gap-8, space-y-8                                          ║
  ╚═══════════════════════════════════════════════════════════════════════════╝
-->

| 용도 | 값 | 클래스 |
|------|-----|--------|
| 섹션 간격 | 1.5rem (24px) | `space-y-6` |
| 카드 간격 | 1.5rem (24px) | `gap-6` |
| 작은 간격 | 1rem (16px) | `gap-4` |

### 반응형 브레이크포인트

<!-- 이 섹션은 범용적으로 사용 가능합니다 (Tailwind 기본값) -->

```tsx
// 모바일 퍼스트 접근
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3">
  {/*
    모바일: 1열
    태블릿 (md: 768px+): 2열
    데스크톱 (lg: 1024px+): 3열
  */}
</div>
```

---

## 🧩 컴포넌트

### PageHeader (범용)

<!-- 이 컴포넌트는 범용적으로 사용 가능합니다 -->

```tsx
import { PageHeader } from '@/components/common/PageHeader';

<PageHeader
  title="페이지 제목"
  description="선택적 설명" // optional
  action={<Button>액션</Button>} // optional
/>
```

### EmptyState (범용)

<!-- 이 컴포넌트는 범용적으로 사용 가능합니다 -->

```tsx
import { EmptyState } from '@/components/common/EmptyState';
import { FileText } from 'lucide-react';

<EmptyState
  icon={<FileText className="h-12 w-12" />} // optional
  title="데이터가 없습니다"
  description="새 항목을 추가하세요" // optional
  action={<Button>추가하기</Button>} // optional
/>
```

### 도메인 특화 컴포넌트

<!--
  ╔═══════════════════════════════════════════════════════════════════════════╗
  ║  [OPTIONAL: 도메인 특화 컴포넌트]                                          ║
  ║  프로젝트 도메인에 맞는 특화 컴포넌트를 정의하세요.                         ║
  ║                                                                           ║
  ║  예시:                                                                    ║
  ║  - 재무 앱: KpiCard, TransactionItem, BudgetProgress                      ║
  ║  - 이커머스: ProductCard, CartItem, PriceTag                              ║
  ║  - 소셜 앱: UserCard, PostCard, CommentItem                               ║
  ╚═══════════════════════════════════════════════════════════════════════════╝
-->

```tsx
/* 현재 프로젝트: KpiCard (재무 지표 카드) */
import { KpiCard } from '@/components/common/KpiCard';

<KpiCard
  title="월 수입"
  amount={1500000}
  changeRate={12.5} // optional
  variant="income" // 'income' | 'expense' | 'net'
/>
```

### Card 컴포넌트 (shadcn/ui)

<!-- 이 섹션은 범용적으로 사용 가능합니다 -->

```tsx
import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card';

<Card>
  <CardHeader>
    <CardTitle>제목</CardTitle>
  </CardHeader>
  <CardContent>
    {/* 내용 */}
  </CardContent>
</Card>
```

**패딩 표준:**
- 일반 카드: `p-6`
- 컴팩트 카드: `p-3`
- 빈 상태: `p-12`

---

## 🌓 다크모드

### 다크모드 지원 원칙

<!-- 이 섹션은 범용적으로 사용 가능합니다 -->

모든 컴포넌트는 다크모드를 지원해야 합니다.

```tsx
// Good: 다크모드 지원
<div className="bg-red-50 dark:bg-red-950/30 border-red-200 dark:border-red-800">
  <span className="text-red-600 dark:text-red-400">경고</span>
</div>

// Bad: 라이트모드만 지원
<div className="bg-red-50 border-red-200">
  <span className="text-red-600">경고</span>
</div>
```

### 다크모드 컬러 패턴

| 요소 | Light Mode | Dark Mode |
|------|------------|-----------|
| 배경 | `bg-{color}-50` | `bg-{color}-950/30` |
| 테두리 | `border-{color}-200` | `border-{color}-800` |
| 텍스트 | `text-{color}-600` | `text-{color}-400` |

### CSS 변수 다크모드

<!--
  ╔═══════════════════════════════════════════════════════════════════════════╗
  ║  [THEME: 다크모드 컬러 조정]                                               ║
  ║  다크모드에서는 밝기(Lightness)를 높여야 가독성이 좋습니다.                 ║
  ║  일반적으로 Light 모드 대비 10-15% 밝기 증가를 권장합니다.                  ║
  ╚═══════════════════════════════════════════════════════════════════════════╝
-->

```css
:root {
  --status-success: 142 76% 36%;  /* Light: 36% */
}

.dark {
  --status-success: 142 70% 45%; /* Dark: 45% (+9%) */
}
```

---

## ♿ 접근성

<!-- 이 전체 섹션은 범용적으로 사용 가능합니다 -->

### 터치 타겟

모바일 접근성을 위해 최소 44x44px 터치 타겟을 보장합니다.

```tsx
<button className="min-h-[44px] min-w-[44px]">
  <Icon className="h-4 w-4" />
</button>
```

### 명암비

WCAG 2.1 AA 기준을 충족합니다:
- 일반 텍스트: 최소 4.5:1
- 큰 텍스트 (18pt+): 최소 3:1

### 시맨틱 HTML

```tsx
// Good: 시맨틱 태그 사용
<nav>
  <ul>
    <li><Link to="/page">페이지</Link></li>
  </ul>
</nav>

// Bad: div 남용
<div>
  <div>
    <div><a href="/page">페이지</a></div>
  </div>
</div>
```

### ARIA 레이블

```tsx
// 아이콘 버튼에는 aria-label 필수
<Button variant="ghost" size="icon" aria-label="이전">
  <ChevronLeft className="h-4 w-4" />
</Button>

// 숨겨진 설명
<DialogDescription className="sr-only">
  폼에 대한 설명을 입력합니다.
</DialogDescription>
```

---

## 📦 컴포넌트 라이브러리

<!-- 이 섹션은 범용적으로 사용 가능합니다 -->

### shadcn/ui

프로젝트는 shadcn/ui를 기반으로 합니다.

**사용 가능한 컴포넌트:**
- Button, Card, Dialog, Input, Select, Label 등

**커스터마이징:**
- 테마: `/src/index.css`의 CSS 변수 수정
- 컴포넌트: `/src/components/ui/` 파일 직접 수정

### Lucide React Icons

아이콘은 Lucide React를 사용합니다.

```tsx
import { FileText, TrendingUp, AlertCircle } from 'lucide-react';

<FileText className="h-4 w-4" />
<TrendingUp className="h-5 w-5 text-green-600" />
```

**표준 크기:**
- 작은 아이콘: `h-4 w-4` (16px)
- 중간 아이콘: `h-5 w-5` (20px)
- 큰 아이콘: `h-12 w-12` (48px)

---

## 🔧 개발 워크플로우

<!-- 이 섹션은 범용적으로 사용 가능합니다 -->

### 새 컴포넌트 생성 시

1. **디자인 확인**: 기존 컴포넌트 재사용 가능한지 확인
2. **다크모드 지원**: 모든 색상에 dark: 클래스 추가
3. **CSS 변수 사용**: 하드코딩 대신 CSS 변수 사용
4. **타입 정의**: TypeScript 인터페이스 명확히 정의
5. **접근성 고려**: ARIA 레이블, 시맨틱 HTML 사용

### 새 페이지 생성 시

```tsx
import { PageHeader } from '@/components/common/PageHeader';
import { EmptyState } from '@/components/common/EmptyState';
import { LAYOUTS, SPACING } from '@/lib/constants/layouts';

export default function NewPage() {
  return (
    <div className={SPACING.section}>
      <PageHeader
        title="페이지 제목"
        action={/* 액션 버튼 */}
      />

      {/* 메인 컨텐츠 */}
      <div className={LAYOUTS.threeColumn}>
        {/* 카드들 */}
      </div>

      {/* 빈 상태 */}
      {data.length === 0 && (
        <EmptyState
          icon={<Icon className="h-12 w-12" />}
          title="데이터가 없습니다"
          description="설명"
        />
      )}
    </div>
  );
}
```

---

## 📚 참고 자료

### 내부 리소스
- `/src/index.css` - CSS 변수 정의
- `/src/lib/constants/layouts.ts` - 레이아웃 상수
- `/src/components/common/` - 공통 컴포넌트

### 외부 리소스
- [shadcn/ui](https://ui.shadcn.com/) - 컴포넌트 라이브러리
- [Tailwind CSS](https://tailwindcss.com/) - CSS 프레임워크
- [Lucide Icons](https://lucide.dev/) - 아이콘 라이브러리
- [WCAG 2.1](https://www.w3.org/WAI/WCAG21/quickref/) - 접근성 가이드라인

---

## ✅ 체크리스트

새 기능을 추가하거나 수정할 때 다음을 확인하세요:

- [ ] CSS 변수 사용 (하드코딩 금지)
- [ ] 다크모드 지원 (모든 색상에 dark: 클래스)
- [ ] 타이포그래피 규칙 준수 (페이지 제목 text-2xl 등)
- [ ] 표준 레이아웃 패턴 사용 (LAYOUTS 상수)
- [ ] 표준 간격 사용 (SPACING 상수)
- [ ] 공통 컴포넌트 재사용 (PageHeader, EmptyState 등)
- [ ] 숫자/금액 표시에 tabular-nums 사용
- [ ] 터치 타겟 최소 44x44px
- [ ] ARIA 레이블 추가 (필요시)
- [ ] 시맨틱 HTML 사용

---

<!--
  ╔═══════════════════════════════════════════════════════════════════════════╗
  ║  커스터마이징 요약 (Quick Reference)                                       ║
  ╠═══════════════════════════════════════════════════════════════════════════╣
  ║                                                                           ║
  ║  🎨 [THEME] 테마 커스터마이징:                                             ║
  ║     • 브랜드 컬러 (primary, secondary)                                    ║
  ║     • 폰트 패밀리                                                         ║
  ║     • 간격 값 (컴팩트/기본/여유로운)                                       ║
  ║     • 다크모드 밝기 조정                                                  ║
  ║                                                                           ║
  ║  📦 [OPTIONAL] 도메인 특화:                                               ║
  ║     • 도메인 컬러 (재무: income/expense, 건강: healthy/critical 등)       ║
  ║     • 도메인 유틸리티 클래스 (.amount-income 등)                          ║
  ║     • 도메인 컴포넌트 (KpiCard, ProductCard 등)                           ║
  ║                                                                           ║
  ║  ✅ 범용 (수정 불필요):                                                    ║
  ║     • Status 컬러 (success, warning, danger, info)                        ║
  ║     • 타이포그래피 스케일                                                 ║
  ║     • 레이아웃 패턴                                                       ║
  ║     • 다크모드 패턴                                                       ║
  ║     • 접근성 가이드라인                                                   ║
  ║     • PageHeader, EmptyState 컴포넌트                                     ║
  ║                                                                           ║
  ╚═══════════════════════════════════════════════════════════════════════════╝
-->

**마지막 업데이트:** 2025-12-29
**버전:** 1.1.0
